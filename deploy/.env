# 生产环境配置（服务器现场文件，已 gitignore）
# 仓库根目录：docker compose -f deploy/docker-compose.yml --env-file deploy/.env up -d --build

# ============ MySQL ============
MYSQL_ROOT_PASSWORD=123456
MYSQL_DATABASE=blog
MYSQL_USER=blog
MYSQL_PASSWORD=123456

# ============ JWT（32 位随机，已生成）============
JWT_SECRET=3BoExYN4L16rlWy0OjXRn2I8qSf7UzAg
JWT_EXPIRE_MINUTES=10080

# ============ CORS：与浏览器地址栏来源一致，逗号分隔无空格 ============
APP_CORS_ALLOWED_ORIGINS=https://www.weihanyinian.cn,http://www.weihanyinian.cn

# ============ 对外 HTTP 端口 ============
HTTP_PORT=80

# ============ 可选 ============
VIEW_COUNTER_REDIS=false
BOOTSTRAP_ADMIN_PASSWORD=
AI_COMPANION_ENABLED=true
AI_COMPANION_API_KEY=sk-62bbad70ae564b7a95945d56cad71969

# ============ 仅 docker-compose.images 流程 ============
IMAGE_TAG=latest
