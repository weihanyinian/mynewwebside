# 留空：请求使用相对路径 /api（浏览器在 :5173 则走 Vite 代理；在 :88 则走 Nginx → 8080）
VITE_API_BASE_URL=
# 仅当用 docker/proxy/nginx.unified-88.host-dev.conf 从 http://localhost:88 访问 Vite 时取消注释：
# VITE_DEV_HMR_CLIENT_PORT=88
